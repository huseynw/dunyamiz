[build]
  command = "npm install && npm run build"
  publish = "dist"

[dev]
  port = 8888
  targetPort = 5173

[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/.netlify/functions/github-videos"
  [headers.values]
    Access-Control-Allow-Origin = "*"
